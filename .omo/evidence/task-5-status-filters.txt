Task: Task 5 - Desktop Layout Rework
Scenario: Status filter chips work correctly
Date: 2026-05-22

Test Command: flutter test test/plugins/thoughts/providers/thoughts_providers_test.dart

Expected Result: Each chip activates correct filter.

Actual Result: Provider-level filter behavior verified by passing tests:

1. "pinned" status filter (thoughtStatusFilterProvider → ThoughtStatusFilter.pinned)
   - Test: "composes archive, status, tag, then debounced search filters" ✓
   - Sets status=pinned, tag="work", search="ALPHA" → returns only pinned work items

2. "archived" status filter (thoughtStatusFilterProvider → ThoughtStatusFilter.archived)
   - Test: "archived status filter selects the archive bucket" ✓
   - Sets status=archived → archiveFilterProvider toggled to true
   - Returns only archived thoughts

3. "withImages" status filter
   - Test: "withImages status uses image metadata without changing schema" ✓
   - Returns only thoughts with imagePaths

4. "all" (default) status filter
   - Default value (ThoughtStatusFilter.all) - no filtering applied
   - All unarchived thoughts returned

The ThoughtStatusFilter enum has 4 values:
  - all (default)
  - pinned
  - withImages
  - archived (toggles archiveFilterProvider)

The status filter bar (ThoughtFilterBar) maps:
  - "全部" chip → statusFilter=all, archiveFilter=false
  - "置顶" chip → statusFilter=pinned
  - "有图片" chip → statusFilter=withImages
  - "归档" chip → statusFilter=archived (also sets archiveFilter=true)

Pass/Fail: PASS
