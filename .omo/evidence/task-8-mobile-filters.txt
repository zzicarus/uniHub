Task: Task 8 - Mobile Layout Parallel Update
Scenario: Mobile filters work with same providers as desktop
Date: 2026-05-22

Expected Result: Mobile and desktop use same provider chain.

Verification: Code inspection confirms that both layouts use the same providers.

Shared provider chain (same for mobile and desktop):
- thoughtSearchQueryProvider (StateProvider<String>)
- thoughtSearchDebouncedProvider (debounced 300ms)
- thoughtStatusFilterProvider (StateProvider<ThoughtStatusFilter>)
- tagFilterProvider (StateProvider<String?>)
- archiveFilterProvider (StateProvider<bool>)
- thoughtsListProvider (composes all filters)

The mobile layout (thoughts_mobile_layout.dart) uses:
- ConsumerWidget pattern with WidgetRef to access providers directly
- ThoughtFilterBar for status chips (same as desktop)
- ThoughtTagFilterBar for tag chips (same as desktop)
- ThoughtSearchBox for search (same shared widget)

The thoughtsListProvider composes filters correctly as verified by passing tests:
- test "composes archive, status, tag, then debounced search filters" ✓
- test "search matches plain text content and tags case-insensitively" ✓
- test "withImages status uses image metadata without changing schema" ✓
- test "tag filter alone works correctly" ✓ (from integration test)

The mobile layout uses the same thoughtStatusFilterProvider, thoughtSearchQueryProvider,
tagFilterProvider, and archiveFilterProvider as the desktop layout. No mobile-specific
providers exist.

Pass/Fail: PASS
