# {module_path} — {module_name}

> 本文件由 sync-knowledge 自动创建，请在首次同步时补充模块描述和核心约定。

## 近期变更

> 本 section 由 sync-knowledge 按时间倒序追加。格式参考：

```
### YYYY-MM-DD: [改动类型] 标题

- **改动文件**: 路径列表
- **原因**: 为什么做这个改动
- **影响**: 对其他模块或插件的潜在影响
- **关键约定**: 如果有新的"必须遵守的规则"，重点列出
```
